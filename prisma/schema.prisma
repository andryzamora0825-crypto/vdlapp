generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Voucher {
  id            String   @id @default(uuid())
  userId        String?
  comprobante   String
  monto         String
  createdAt     DateTime @default(now())
}
